// import path from 'path'
// import fs from 'fs'
// import { getFile } from '../src/commands/get-file'
test('getFile--获取注释', () => {
  // 1. 随机创建一个文件
  const str = ` // 我就是个注释
  <script setup>
import UserRuler from '@/unuse/components/user-rulerts'
</script>`
  // const file = path.resolve(__dirname, '\\unuse\\AppFileTest.vue')
  // fs.writeFile(file, str, { encoding: 'utf8' },  () => {
  //   const obj=getFile(file)
  // expect(obj).toEqual('./components/user-rulerts.vue')
  // })

})
