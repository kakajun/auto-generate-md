name: test

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Rust 🔧
        uses: dtolnay/rust-toolchain@stable

      - name: Run tests
        run: cargo test
